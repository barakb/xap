<?xml version="1.0" encoding="UTF-8"?>
<!--
****************************************************************************************
	 Generated by gs xml generator	on $currDate
****************************************************************************************
-->
<!DOCTYPE gigaspaces-mapping PUBLIC "-//GIGASPACES//DTD GS//EN" "http://www.gigaspaces.com/dtd/9_7/gigaspaces-metadata.dtd">
<gigaspaces-mapping>
#foreach ($className in $classes)
	<class name="${className}" persist="${isPersistent}" replicate="${isReplicatable}" fifo="${isFifo}" mapping-type="${applicationType}" inherit-indexes="${indexingMode}">
			#if ($fieldsOfClasses.size()> 0)
				#set ($fields = $fieldsOfClasses.remove(0))
					#foreach ($field in $fields)
						#set ($pk = "false")
						#set ($pk = $fieldsPk.get(${field}))
		#if($pk =="false")
		<property name="${field}" index="none"/>
		#else
		<id name="${field}" auto-generate="false"/>
		#end
					#end
			#end
			#if ($refClasses.size()> 0)
			#set ($toRemove = $refClasses.remove(0))
		<reference class-ref="${toRemove}"/>

			#end
	</class>
#end
</gigaspaces-mapping>


